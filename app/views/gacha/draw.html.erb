<div class="container d-flex flex-column align-items-center justify-content-center text-center" style="min-height: 100vh; background-color: #e6f0ff;">
  <h1 class="mb-4 text-primary">Gacha#draw</h1>

  <% if @card %>
    <div class="card p-4 mb-4 shadow" style="background-color: #ffffff; border: 1px solid #b3d1ff;">
      <strong class="mb-3 d-block text-primary">結果：</strong>
      <%= image_tag @card.image, size: '218x270', class: 'mb-3' %>
      <p class="text-secondary">st = <%= @card.st %></p>
    </div>
  <% else %>
    <p class="text-danger">景品が登録されていません。</p>
  <% end %>

  <%= link_to 'もう一度引く', draw_path, class: 'btn btn-outline-primary mt-3' %>
</div>
